IPEORG MLBB Verification Bot – PromptGoal:
Create a Discord bot for the IPEORG MLBB India server that verifies users using Game ID + DM OTP + Admin approval, then assigns roles and sends result DMs with MLBB details.��Core Flow (User Side)Join serverNew users get role: Unverified (no access to main channels).Start verificationIn #📋verify channel, user runs a command or clicks a button:Example: /verifyBot replies in channel:Asks for MLBB Game ID (9-digit from Profile → Copy ID).�Submit Game IDUser sends their Game ID.Bot checks basic format (9 digits) and stores: userID + Game ID as a pending request.Send OTP via Discord DMBot generates a random 6-digit OTP.Bot DMs the user:“Your IPEORG MLBB verification code is XXXXXX. Reply with this code in this DM within 2 minutes.”User replies OTP in DMIf code matches and not expired:User gets temporary role: Pending Verification.A verification ticket is created for admins.If wrong/expired:Bot asks them to restart /verify.Admin Flow (Staff Side)Admin dashboard channelChannel: #🔧admin-dashboard (visible only to staff).For each verified OTP, bot posts an embed with:Discord user mentionMLBB Game IDTime of requestOptional: basic MLBB info (IGN, rank, etc., if using a public MLBB stats API).�Admin actions on each request
Each request message shows clear action options (buttons or reactions):✅ ApproveBot:Removes Unverified/Pending Verification roles.Gives Verified MLBB role.Sends DM to user with:“Congratulations, you are verified on IPEORG!”MLBB summary: IGN, rank, server, etc. (if available).A small status text and optional MLBB image (e.g., rank badge).�❌ Deny – Fake/Invalid IDBot:Keeps user Unverified (or removes Pending).DMs user:“Verification denied: Invalid or fake Game ID. Please check your 9-digit ID from Profile → Copy ID and try again.”❌ Deny – Wrong RegionBot:DMs user:“Verification denied: This server only accepts accounts from the allowed MLBB region (e.g., India).”📝 Custom Deny ReasonAdmin can type a custom short reason (e.g., “Rank too low for this tournament”, “Account too new”, etc.).Bot DMs user with that reason.LoggingAll actions (approve/deny + reason) are logged in #🛡️staff-logs for audit and anti-alt tracking.�